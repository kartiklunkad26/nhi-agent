# ============================================
# NHI Agent Configuration
# ============================================

# ========== INSECURE MODE (Admin Credentials) ==========
# These credentials have admin/full IAM access and can see all users
# Used when secure mode is OFF (default)

# Option 1: Use AWS Profile (recommended for local development)
AWS_PROFILE=default
AWS_REGION=us-east-1

# Option 2: Use explicit credentials (alternative to profile)
# AWS_ACCESS_KEY_ID=AKIA...your-admin-key
# AWS_SECRET_ACCESS_KEY=...your-admin-secret

# ========== SECURE MODE (User-Specific Credentials) ==========
# Each user has their own AWS access keys with minimal IAM permissions
# Used when secure mode is ON

# Format: AWS_USER_{username}_KEY and AWS_USER_{username}_SECRET
# Replace {username} with the exact IAM username

# aws-admin-user credentials (minimal permissions for their own data)
AWS_USER_aws-admin-user_KEY=AKIA...user1-access-key
AWS_USER_aws-admin-user_SECRET=...user1-secret-key

# terraform-user credentials
AWS_USER_terraform-user_KEY=AKIA...user2-access-key
AWS_USER_terraform-user_SECRET=...user2-secret-key

# kartik-aws-user credentials
AWS_USER_kartik-aws-user_KEY=AKIA...user3-access-key
AWS_USER_kartik-aws-user_SECRET=...user3-secret-key

# test-user credentials
AWS_USER_test-user_KEY=AKIA...user4-access-key
AWS_USER_test-user_SECRET=...user4-secret-key

# demo-user credentials
AWS_USER_demo-user_KEY=AKIA...user5-access-key
AWS_USER_demo-user_SECRET=...user5-secret-key

# ========== OpenAI Configuration ==========
OPENAI_API_KEY=sk-...your-openai-key

# ========== CORS Configuration ==========
CORS_ORIGINS=http://localhost:8080,http://localhost:5173

# ============================================
# NOTES:
# ============================================
# 1. In INSECURE mode, the app uses AWS_PROFILE or AWS_ACCESS_KEY_ID
# 2. In SECURE mode, the app uses AWS_USER_{username}_KEY for the selected user
# 3. Each user's credentials should have MINIMAL IAM permissions:
#    - iam:GetUser (for their own user only)
#    - iam:ListAccessKeys (for their own keys only)
# 4. Example minimal IAM policy for a user:
#    {
#      "Version": "2012-10-17",
#      "Statement": [{
#        "Effect": "Allow",
#        "Action": ["iam:GetUser", "iam:ListAccessKeys"],
#        "Resource": "arn:aws:iam::ACCOUNT_ID:user/${aws:username}"
#      }]
#    }
